pipeline {
    agent any

    stages {
        stage("Build") {
            withGradle {
                steps {
                    sh 'chmod +x gradlew'
                    sh './gradlew clean build'
                }
            }
        }
    }
}